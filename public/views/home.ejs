<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    
    <!--Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!---->
    <link rel="stylesheet" href="/style.css">
    <title>Link-Shorter</title>

    <style>
        .mynav{
            padding: 4px;
            justify-content: space-between;
        }
        .title{
            color: white;
            padding: 4px; 
            text-decoration: none;
            font-size: 36px;
        }
        .title-link:hover{
            color: #C0C0C0;
        }
        .content{
            font-size: 40px;
            margin:20px;
        }
    </style>
</head>
<body>
    
    <nav class="navbar navbar-dark bg-dark mynav">
        <a class="title title-link" href="<%= hostAddress %>">Link-Shorter</a>
        <p class="title">Short your links with us. </p>
        <a class="title title-link" target="_blank" href="https://github.com/R3SP31T4/link-shorter">Github</a>
    </nav>

    <div class="wrapper">
        <div class="content">
            
            <form class="input-group input-group-lg" method="post" action="<%= hostAddress %>">
                
                <input class="form-control" place-holder="Write Here." value="<%= link %>" id="link" name="link" required>
                <% if(disabled == true){%>
                <button class="btn btn-success" type="button" onclick="copy()">Copy</button>
                <%}else{%> 
                    <button class="btn btn-secondary" disabled>Copy</button>
                <%}%>
                <button class="btn btn-primary" type="submit">Short</button>

            </form>

        </div>
    </div>
    <script>
        function copy(){
            var content = document.getElementById('link').value;

            navigator.clipboard.writeText(content).catch(err => {
                console.log("Error: " + err)
            });
        }
    </script>
</body>
</html>